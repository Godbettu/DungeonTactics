using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class LoadSceneStageC : MonoBehaviour
{
    public void CStage1(int sceneIndex)
    {
        SceneManager.LoadScene(sceneIndex);
    }

    public void CStage2(int sceneIndex)
    {
        SceneManager.LoadScene(sceneIndex);
    }

    public void CStage3(int sceneIndex)
    {
        SceneManager.LoadScene(sceneIndex);
    }
}
