using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class LoadSceneStageD : MonoBehaviour
{
    public void DStage1(int sceneIndex)
    {
        SceneManager.LoadScene(sceneIndex);
    }

    public void DStage2(int sceneIndex)
    {
        SceneManager.LoadScene(sceneIndex);
    }

    public void DStage3(int sceneIndex)
    {
        SceneManager.LoadScene(sceneIndex);
    }
}
